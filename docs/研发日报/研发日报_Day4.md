# 大模型内容安全与质量评估智能体 · 研发日报

---

|          |                                          |
|----------|------------------------------------------|
| **日期** | 2026年7月1日（周二）                      |
| **课题** | 内容保真度与治理质量评估智能体（LLM-as-Judge） |
| **阶段** | Phase 3 — 数据集扩充与 Prompt 调优 + Phase 6 — 端到端集成测试 |
| **日报编号** | Day 4 / 10                           |
| **组长** | 李首澎                                   |
| **组员** | 阿思晗、由靖喆                            |

---

## 一、组员进度汇报

### 1.1 组员：阿思晗

#### 今日任务：P3-4 评估数据集扩充 + P3-5 Prompt 调优迭代 + P6-1 端到端集成测试

今日按照计划书 Phase 3 收尾和 Phase 6 测试任务展开工作。

**上午 — Phase 3 收尾**

首先完成了 **P3-4 评估数据集扩充**：在 `data/eval_dataset.json` 中补充了新的人工标注样本，覆盖量化指标篡改、段落大幅删除、纯格式优化三类典型场景，确保数据集在优秀治理（≥0.8）、中等质量（0.5-0.8）和低质量（<0.5）三档之间分布均衡。每条样本均包含完整的 `human_label` 字段（overall_score + flaws 列表 + severity 分级）。

随后进行 **P3-5 Prompt 调优迭代**：基于前三天校准测试中发现的高偏差样本，对 `app/prompts.py` 中的 System Prompt 进行了多轮定向调优。针对三类偏差场景分别修改：
- 数值敏感性：在 `mis_edit` 类型说明中增加"数量级变化（如 120万→12万）必须标记为 critical"；
- 格式优化豁免：增加"纯标点/空格/段落对齐调整不扣除语义一致性分"；
- 过度清洗权重：增加"删除原文 30% 以上内容判定为 critical 级 over_clean"。

每轮调优后重新运行 `run_calibration.py` 验证，通过 2-3 轮迭代确认评分一致性有明显改善。

**下午 — Phase 6：端到端集成测试**

完成了 **P6-1 端到端集成测试**：对从 API 入口（`main.py` → `app/routes.py`）到 LLM 调用（`app/engine.py`）到 JSON 解析到结果渲染的完整链路进行了串联验证。确认各模块之间的数据流一致，评估结果能够正确从后端传递到前端展示。

| 任务 | 完成状态 |
|------|----------|
| P3-4 评估数据集扩充（含人工标注 Ground Truth） | ✅ 已完成 |
| P3-5 Prompt 调优迭代（2-3 轮，目标 Pearson r ≥ 0.8） | ✅ 已完成 |
| P6-1 端到端集成测试（API → LLM → 解析 → 渲染） | ✅ 已完成 |

**计划工时：4h　　实际工时：约 4.5h　　偏差：超时约 0.5h（Prompt 调优迭代较多）**

---

### 1.2 组员：由靖喆

#### 今日任务：P6-2 边界测试 + P6-3 异常测试 + 测试报告整理

今日配合阿思晗的集成测试工作，重点补充边界场景和异常场景的测试覆盖。

**下午 — Phase 6：边界测试与异常测试**

首先完成了 **P6-2 边界测试**，在 `tests/test_day6_minimal.py` 中补充了以下边界场景的测试用例：
- 空文本输入：验证 `run_full_evaluation([])` 返回合理的壳结构，不报错不白屏；
- 超长文本：验证系统能够处理较长文本输入而不超时或崩溃；
- 特殊字符：验证包含 HTML 标签、emoji、特殊标点的文本能够正常处理；
- 中英混合：验证中英文混合文本的锚点标记和评估逻辑正确。

随后完成了 **P6-3 异常测试**，补充了以下异常场景的测试：
- API Key 错误：验证系统返回友好的错误提示而非白屏；
- 网络超时：验证超时情况下有合理的重试和降级机制；
- JSON 畸形：验证 LLM 返回非标准 JSON 时，三层解析兜底逻辑（直接解析 → 清洗 think 标签 → 大括号定位）能够正确处理。

最后整理了测试报告，将端到端测试、边界测试和异常测试的结果汇总为阶段验收支撑材料。

| 任务 | 完成状态 |
|------|----------|
| P6-2 边界测试（空文本 / 超长文本 / 特殊字符 / 中英混合） | ✅ 已完成 |
| P6-3 异常测试（API Key 错误 / 网络超时 / JSON 畸形） | ✅ 已完成 |
| 测试报告整理 | ✅ 已完成 |

**计划工时：2h　　实际工时：约 2.5h　　偏差：超时约 0.5h（异常场景用例编写较多）**

---

## 二、组长汇总

### 2.1 今日整体进度说明

根据研发计划书安排，Day 4 的核心任务是完成 **端到端集成测试 + 边界/异常 case**（对应 Phase 6 前 3 项），同时收尾 Phase 3 的剩余任务。今日完成了 P3-4（数据集扩充）、P3-5（Prompt 调优迭代）、P6-1（端到端集成测试）、P6-2（边界测试）和 P6-3（异常测试）共 5 项任务。

Phase 3 的全部 5 项任务（P3-1 至 P3-5）至此已全部完成，Phase 6 的测试工作也已完成前 3 项。里程碑 M2（指标全部达标）和 M3（集成测试通过）的前置条件已齐备。

### 2.2 组员任务完成情况汇总

| 姓名 | 今日任务 | 完成状态 | 备注 |
|------|----------|----------|------|
| 阿思晗 | P3-4 数据集扩充 + P3-5 Prompt 调优 + P6-1 端到端测试 | ✅ 全部完成 | 超时 0.5h |
| 由靖喆 | P6-2 边界测试 + P6-3 异常测试 + 测试报告 | ✅ 全部完成 | 超时 0.5h |
| 李首澎（组长） | 测试范围把关 + 调优评审 + 验收统筹 | ✅ 全部完成 | 见下节 |

### 2.3 组长今日工作内容

1. **数据集质量审核**：审核新增样本的标注质量，确认 overall_score 和 flaws 标注与实际文本情况一致，样本分布符合 4:5:3 的设计原则。
2. **Prompt 调优方向把关**：审查每轮调优的修改内容，确认新增规则与现有评分框架逻辑一致，避免产生规则冲突。重点验证调优后高偏差样本数量是否减少。
3. **测试范围审查**：确认端到端测试覆盖了主链路（API → LLM → 解析 → 渲染），边界测试覆盖了四类边界场景，异常测试覆盖了三类异常场景，满足计划书 P6-1/P6-2/P6-3 的要求。
4. **阶段验收统筹**：对 Day 1 至 Day 4 的成果进行归拢，确认 Phase 2 全部完成、Phase 3 全部完成、Phase 6 测试前 3 项完成，为后续报告生成和 UI 精修做好准备。

**计划工时：6h　　实际工时：约 6h　　偏差：持平**

---

## 三、今日研发过程记录

### 3.1 P3-4 评估数据集扩充

在 `data/eval_dataset.json` 中补充了新的人工标注样本，扩充后数据集覆盖以下场景：

| 场景类型 | 样本数 | 典型特征 |
|----------|--------|----------|
| 优秀治理（≥0.8） | ~40% | 仅格式优化，内容保真度高 |
| 中等质量（0.5-0.8） | ~35% | 轻微信息丢失或表述偏差 |
| 低质量（<0.5） | ~25% | 数值篡改、大段删除、严重误改 |

每条样本包含完整的 `human_label` 字段：
```json
{
  "human_label": {
    "overall_score": 0.45,
    "flaws": [
      {
        "type": "mis_edit",
        "severity": "critical",
        "description": "营业收入数值被错误修改：120万元→12万元",
        "location": { "segment_id": "before_1", "char_offset": 12 }
      }
    ]
  }
}
```

### 3.2 P3-5 Prompt 调优迭代

进行了 2-3 轮调优迭代，每轮流程为：分析高偏差样本 → 定向修改 Prompt → 重新校准 → 对比改善效果。

**第一轮**：针对数值敏感性不足（120万→12万未被识别为 critical），在 `mis_edit` 说明中增加量化约束。调优后 Pearson r 从 0.61 提升至约 0.72。

**第二轮**：针对格式优化误罚（纯标点调整被扣语义分），增加格式豁免条款。调优后高偏差样本从 3 条减少至 1 条，Pearson r 提升至约 0.78。

**第三轮**：针对过度清洗识别不足（删除 40% 内容未被判定为 critical），增加量化阈值。调优后 Pearson r 达到 0.8 以上，里程碑 M1 达标。

### 3.3 P6-1 端到端集成测试

测试链路：`main.py`（FastAPI 服务入口） → `app/routes.py`（路由层，6 个端点） → `app/engine.py`（LLM 调用 + JSON 解析） → `app/reporter.py`（综合报告生成） → `static/index.html`（前端渲染）。

验证结果：各模块之间数据流一致，评估请求能够正确从 API 入口传递到 LLM、解析为结构化结果、生成综合报告并渲染到前端页面。

### 3.4 P6-2 边界测试

| 测试场景 | 预期行为 | 实际结果 |
|----------|----------|----------|
| 空文本输入 | 返回壳结构，不报错 | ✅ 通过 |
| 超长文本（>5000 字） | 正常评估，不超时 | ✅ 通过 |
| 特殊字符（HTML/emoji） | 正常处理，不转义异常 | ✅ 通过 |
| 中英混合文本 | 锚点标记和评估正确 | ✅ 通过 |

### 3.5 P6-3 异常测试

| 测试场景 | 预期行为 | 实际结果 |
|----------|----------|----------|
| API Key 错误 | 返回友好错误提示 | ✅ 通过 |
| 网络超时 | 重试后返回超时提示 | ✅ 通过 |
| JSON 畸形（LLM 输出非标准 JSON） | 三层兜底解析成功 | ✅ 通过 |

---

## 四、关键产出

| 产出内容 | 对应计划任务 | 说明 |
|----------|-------------|------|
| `data/eval_dataset.json` 扩充 | P3-4 | 新增人工标注样本，覆盖三类场景 |
| `app/prompts.py` 调优 | P3-5 | 2-3 轮迭代，Pearson r ≥ 0.8 |
| 端到端集成测试通过 | P6-1 | API → LLM → 解析 → 渲染全链路 |
| `tests/test_day6_minimal.py` 边界测试 | P6-2 | 空文本 / 超长 / 特殊字符 / 中英混合 |
| 异常测试用例 | P6-3 | API Key 错误 / 超时 / JSON 畸形 |
| 测试报告 | P6-1/2/3 | 阶段验收支撑材料 |

---

## 五、当前进度评估

### 5.1 里程碑进度看板

| 里程碑 | 完成标志 | 目标日 | 当前状态 | 风险等级 |
|--------|----------|--------|----------|----------|
| M1: 校准达标 | Pearson r ≥ 0.8 | Day 2 | ✅ Prompt 调优后达标 | 🟢 低 |
| M2: 指标全部达标 | F1 / 锚点 / 偏置 / 稳定性 通过 | Day 5 | ✅ Phase 2 + Phase 3 全部完成 | 🟢 低 |
| M3: 集成测试通过 | 全流程无 Bug | Day 6 | ⏳ P6-1/2/3 已完成 | 🟢 低 |
| M4: 演示材料完备 | PPT + 脚本 + Demo 就绪 | Day 9 | 🔲 未开始 | 🟢 低 |
| M5: 最终提交 | 全部材料打包提交 | Day 10 | 🔲 未开始 | 🟢 低 |

### 5.2 总体进度状态

> **当前状态：进度超前，Phase 2/3 已全部完成，Phase 6 测试已完成前 3 项。**
>
> Day 4 完成了 Phase 3 收尾（数据集扩充 + Prompt 调优）和 Phase 6 的端到端集成测试、边界测试、异常测试。M1（校准达标）和 M2（指标全部达标）已确认达成，M3（集成测试通过）的前置条件已齐备。

---

## 六、后续安排

后续工作将转入 Phase 4（报告生成与批量评估），重点完成综合评估报告生成器的 7 大模块集成、JSON 报告导出、控制台摘要输出和批量评估脚本。

---

## 七、总结

今天按照研发计划书完成了 Phase 3 的收尾工作（P3-4 数据集扩充、P3-5 Prompt 调优迭代）和 Phase 6 的三项测试任务（P6-1 端到端集成、P6-2 边界测试、P6-3 异常测试）。系统当前已具备完整的评估指标体系和基础测试支撑，进度与计划保持一致，为后续报告生成和批量评估做好了准备。
